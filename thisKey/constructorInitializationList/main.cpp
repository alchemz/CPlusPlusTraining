/*two string method to combine string and number
In order to make the program run faster: 
instead of initialization name the age like this->name=name;

Person::Person(string name, int age):name(name),age(age){}
Person::Person():name("unnamed"),age(11){}
 or public:
 put Person():name("unnamed"), age(11){}
for not complex parameters

*/
